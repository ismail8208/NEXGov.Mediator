namespace NEXGov.Mediator.CompatibilityTests;

// Placeholder smoke test proving the test project builds, references the
// production project, and runs under the configured test infrastructure.
// Real compatibility tests begin once the MediatR-compatible public API
// surface is introduced, proving source-compatibility claims with evidence.
public class ProjectSetupTests
{
    [Fact]
    public void CompatibilityTestProject_IsConfigured()
    {
        Assert.True(true);
    }
}
