namespace NEXGov.Mediator.IntegrationTests;

// Placeholder smoke test proving the test project builds, references the
// production project, and runs under the configured test infrastructure.
// Real integration tests begin once DI integration is introduced.
public class ProjectSetupTests
{
    [Fact]
    public void IntegrationTestProject_IsConfigured()
    {
        Assert.True(true);
    }
}
